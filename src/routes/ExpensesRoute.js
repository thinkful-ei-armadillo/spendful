import React, { Component } from 'react';
import ExpensesPage from '../components/ExpensesPage/ExpensesPage'

export default class ExpensesRoute extends Component {

  render() {
    return (
      <main>
        <ExpensesPage />
      </main>
    );
  }
}